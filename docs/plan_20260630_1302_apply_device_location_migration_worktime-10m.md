# 구현 계획서: 장비 위치 관계 마이그레이션 반영 및 개선

- **작성일시**: 2026-06-30 13:02
- **예상 소요 시간**: 10m

---

## 1. 문제 요약
제안한 '대안 A: 계층형 Nullable 외래키 매핑 구조'를 데이터베이스에 적용하기 위해 컬럼 추가 및 기존 데이터 복구 SQL 마이그레이션 스크립트를 작성하여 `docs` 하위에 저장합니다.

---

## 2. 디자인 요약
- **데이터베이스 컬럼 추가**:
  - `smfr.devices` 테이블에 `building_id` (varchar), `floor_id` (varchar) 컬럼 추가.
  - 각각 `smfr.buildings` 및 `smfr.floors` 테이블을 참조하는 외래키 제약조건(`FOREIGN KEY`) 추가.
- **기존 데이터 마이그레이션**:
  - 기존에 `room_id`만 매핑되어 있던 장비들의 `floor_id`와 `building_id`를 호실 및 층 계층 테이블을 역으로 추적(JOIN)하여 자동으로 채워주는 마이그레이션 UPDATE 쿼리 추가.

---

## 3. 구현 계획
- **Step 1: SQL 마이그레이션 파일 작성**
  - `docs/migration_device_location.sql` 생성.
  - 컬럼 추가 DDL 및 외래키 제약조건 생성 쿼리 포함.
  - 기존 장비 데이터의 부모 계층 복구 업데이트 쿼리 포함.
