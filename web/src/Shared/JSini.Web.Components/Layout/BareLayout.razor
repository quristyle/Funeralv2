@*
    사이드바도 헤더도 없는 레이아웃. 로그인·오류처럼 아직 로그인하지 않았거나
    메뉴를 그릴 수 없는 화면이 쓴다.

    MainLayout 은 IMenuProvider 를 주입받는데, 로그인 전에는 메뉴를 읽을 수
    없으므로(토큰이 없다) 같은 레이아웃을 쓸 수 없다.
*@

@inherits LayoutComponentBase

<div class="jsini-bare">
    @Body
</div>
