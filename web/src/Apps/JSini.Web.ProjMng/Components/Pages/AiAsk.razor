@page "/projmng/ai/ask"
@attribute [RouteKey("projmng.ai.ask")]

@*
    「빠른 지시」 화면. **알맹이는 `AiAskPanel` 이 들고 있다.**

    여기 남는 것은 셋뿐이다 — 주소(`@@page`) · 메뉴와 잇는 열쇠(`RouteKey`) ·
    브라우저 탭 제목. 같은 알맹이를 헤더의 서랍(`QuickAskDrawer`)도 쓰는데,
    그쪽은 주소도 탭 제목도 갖지 않는다. 왜 갈랐는지는 그 부품 머리말에.
*@

<PageTitle>빠른 지시 · 프로젝트관리</PageTitle>

<AiAskPanel />
