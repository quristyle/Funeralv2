@*
    화면 위쪽 안내 줄.

    "이 화면은 조회만 한다" · "등록은 다른 화면에서 한다" 처럼 사용자가 없는
    버튼을 찾지 않게 하는 문구를 담는다. 옛 화면들이 Alert 로 띄우던 자리다.
*@

<div class="pm-notice">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
