@page "/admin"

@*
    포털관리 Piral MFE 모듈의 첫 화면.
*@

<PageTitle>포털관리</PageTitle>

<p>
    Piral.Blazor MFE <code>/admin</code> 모듈이 셸에 로드되었습니다.
</p>

@*
    합성 진단 표는 셸이 들고 있다 — 그 표가 읽는 「기대 목록」이 셸의 타입이라
    업무 모듈에서는 볼 수 없다(의존 규칙 1). 그래서 링크만 둔다.
    `NavLink` 가 아니라 평범한 `a` 인 이유도 같다 — 이 모듈의 경로가 아니다.
*@
<p>
    <a href="/diagnostics">업무 MFE 합성 진단</a> — 어느 모듈이 붙었는지,
    기대와 실제를 나란히 본다. 메뉴를 눌러도 화면이 안 열릴 때 가장 먼저 볼 곳이다.
</p>
