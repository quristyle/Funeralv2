# 메뉴별 이관 점검 이력

Vue 원본(2026-09-05 에 저장소에서 걷어냄)과 Blazor 화면을 **메뉴 하나씩** 맞대어 보고,
빠진 기능을 채우는 작업의 기록이다. 시작 2026-09-06.

원본을 꺼내는 법:

```
git show 009c2835^:fronts/apps/jsini-portal/src/views/<경로>.vue
```

경로 대응표는 [menu-route-map.md](menu-route-map.md) 에 있다(179건 전수, Vue component 컬럼).

## 판정 기준

| 판정 | 뜻 |
|---|---|
| **그대로** | 원본 기능이 다 있다. 손대지 않았다 |
| **보완** | 빠진 기능을 채웠다 |
| **다시 씀** | 원본과 다른 화면이었다. 새로 옮겼다 |
| **의도적 차이** | 원본과 다르지만 그게 맞다고 판단했다. 까닭을 적는다 |

## 진행 규칙

- 메뉴 하나가 끝나면 빌드·아키텍처 테스트를 돌리고 커밋·푸시한다.
- 결정이 필요한 자리는 멈추지 않고 **여기 적고** 정한 대로 간다.
- 운영 DB 를 바꾸는 동작(저장·삭제·이동)은 **실행해 보지 않는다** —
  이 개발 장비의 AuthServer 가 운영 DB 를 직접 본다.

---

## 이력

### 1. `/funeral/player-download` — 플레이어 다운로드 · **다시 씀**

원본: `funeral/player-download/index.vue` (360줄)

**무엇이 달랐나.** 원본은 GitHub Releases 의 설치 파일을 **OS 별 카드 일곱 장**으로
보여 주는 화면인데, Blazor 는 자료실(`auth/help/archives`)의 「플레이어」 분류를
표로 보여 주고 있었다. 자료실에는 아무도 올리지 않고 릴리스 워크플로가 만든 파일은
GitHub 에만 있으므로 **화면이 늘 비어 있었다.** 자료실 화면의 뼈대를 복사해 옮긴
흔적으로 보인다. 짝이 되는 포털관리의 「플레이어 릴리스」가 GitHub 태그를 만드는
화면이라는 점에서도 원본 쪽이 맞다.

**채운 것**

- OS 별 카드 일곱(Windows · 라즈베리파이 · Ubuntu x64 · Ubuntu arm64 ·
  Android TV · 수동 arm64 · 수동 x64), 요구 사항·설치 명령·파일 크기
- 자산 짝짓기를 **배포판과 아키텍처까지** 본다 (`_debian13_` · `_ubuntu24_`).
  `.deb` 로만 고르면 라즈베리파이 카드가 Ubuntu 파일을 집어 온다
- 최신 태그·배포 시각, 「전체 버전 보기」(GitHub Releases 새 창), 새로 고침
- 릴리스가 없을 때/GitHub 이 응답하지 않을 때의 안내

**결정 — GitHub 을 브라우저가 아니라 서버가 부른다.** 원본은 화면에서
`api.github.com` 을 직접 불렀다. AuthServer 에 `GET /system/player-release/latest`
를 새로 두고 거기서 부른다. 토큰이 이미 그 서비스에 있어 저장소가 비공개로 바뀌어도
읽히고, 익명 호출의 시간당 60회 제한(사무실이 한 아이피다)에 걸리지 않는다.
**내려받기 자체는 브라우저가 GitHub 에서 바로 한다** — 파일이 수백 MB 라 포털이
중계하면 그동안 회로가 묶인다.

**함께 고친 곳**

- `AuthServer/DTOs/PlayerReleaseDto.cs` — `PlayerReleaseLatestDto` · `PlayerReleaseAssetDto`
- `AuthServer/Services/PlayerReleaseService.cs` — `GetLatestAsync`
- `AuthServer/Endpoints/PlayerReleaseEndpoints.cs` — `GET /latest`
- `JSini.Web.Funeral/Api/HelpApi.cs` — `GetPlayerLatestAsync` · `PlayerLatest` · `PlayerAsset`
- `funeral.css` — `.fn-pd*` 카드

**확인.** `dotnet build`(web · AuthServer) · 아키텍처 테스트 129건 통과.
화면 확인은 못 했다 — 이 장비에서 GitHub 토큰 설정 여부를 모른다. 설정이 없으면
카드는 다 「파일 없음」으로 뜨고 안내가 붙는다(그 자체가 정상 동작이다).

### 2. `/funeral/building/info` — 건물 관리 · **보완**

원본: `funeral/building/info/index.vue` (287줄)

**빠져 있던 것**

| 무엇 | 어떻게 됐나 |
|---|---|
| 약어(3자리) | 칸이 없었다. **서버가 필수로 받는다**(`BuildingCreateDto.Abbreviation` 에 `[Required]`) — 등록이 400 으로 떨어지고 있었다. 목록 칸과 편집 칸을 붙였다 |
| 건물 전경 사진 (다중) | 올리는 자리가 없었다. 서버는 그동안에도 `building_photo_group_id` 를 들고 있었다 |
| 주차장 안내 이미지 (다중) | 위와 같다 (`parking_photo_group_id`) |

**새로 만든 공용 부품 — `ImageGroup`.** 사진 묶음은 건물 말고도 여러 화면이 쓴다
(고인 가족사진 등). Blazor Common 에 두고 클라이언트(`FileGroupClient`)는
`JSini.Web.Http` 에 뒀다 — 부품이 공용이라 모듈이 등록하면 다른 모듈이 못 쓴다.
AI 대화 클라이언트를 `AddJSiniGateway` 에서 등록하는 것과 같은 이유다.

- 고르는 즉시 올라간다. **첫 장을 올릴 때 서버가 그룹 아이디를 발급**하기 때문이다 —
  저장할 때 올리는 흐름이면 「저장 → 발급 → 다시 저장」이 되어야 한다
- 미리 보기는 셸 중계 경로(`/files/{id}`)를 쓴다. FileServer 가 주는
  `/api/file/...` 는 포털(:5557)에서 404 다
- 대표 사진 지정은 부품에 있지만 건물 화면에서는 끈다(원본에 없다)

**의도적 차이 둘**

1. **회사 고르개를 두지 않았다.** 원본은 위에 `BizSelect(funeralCompany)` 가 있고
   회사별로 건물을 걸러 봤다. Blazor 는 **회사를 서버가 토큰에서 정한다** —
   화면이 회사를 넘기면 남의 회사에 건물을 만드는 길이 열린다. 장례식장 모듈에는
   회사 목록 API 자체가 아직 없다. 여러 회사를 오가는 운영자가 생기면 그때
   붙인다(남는 일에 적어 둔다).
2. **목록의 사진 칸은 썸네일이 아니라 장수다.** 원본은 칸 안에 사진을 가로로
   늘어놓았는데, DevExpress 그리드는 줄 높이가 고정이라 그 자리에서 사진이
   납작해진다. 보고 고치는 것은 편집 창에서 한다.

**함께 고친 곳**: `JSini.Web.Http/FileGroupClient.cs`(신규) ·
`ServiceCollectionExtensions`(등록) · `Components/Data/ImageGroup.razor`(신규) ·
`app.css`(`.jsini-imggroup*`) · `FuneralModels.cs`(`BuildingPhotos` · `ParkingPhotos`)

**확인.** 빌드·아키텍처 테스트 129건 통과. 사진 올리기는 **실제로 해 보지 않았다** —
운영 DB·운영 파일 저장소에 그대로 들어가기 때문이다.

### 3. `/funeral/building/floor` — 층 관리 · **그대로**

원본: `funeral/building/floor/index.vue` (231줄)

원본 기능(건물별 조회 · 등록 · 수정 · 삭제 · 층명/정렬/비고)이 모두 있다.
오히려 **층별 호실 수**와 「호실이 남아 있으면 못 지운다」가 Blazor 에서 늘었다.

**의도적 차이 — 셀 즉시 편집을 편집 폼으로 바꿨다.** 원본은 표의 칸을 눌러
그 자리에서 고치면 곧바로 저장했다(`edit-closed` → `updateFloor`). Blazor 는
화면 백여 개가 `CommGrd` 의 편집 폼 한 가지를 쓴다. 고칠 수 있는 값은 같고,
표에서 잘못 눌러 저장되는 일이 없다.

**회사 고르개**는 건물 관리와 같은 이유로 두지 않았다(2번 항목 참고).

### 4. `/funeral/building/room` — 호실 관리 · **보완**

원본: `funeral/building/room/index.vue` (308줄)

**빠져 있던 것 — 정렬 순서.** 목록에도 편집 칸에도 없었다. 서버는 그동안에도
주고받고 있었고(`RoomDto.SortOrder`) 현황판이 그 값으로 호실 줄을 세운다.
**이름으로 세우면 「101호」보다 「10호」가 앞에 온다**(글자 순서라 그렇다) —
포털에서 순서를 못 고치는 동안 현황판의 차례를 바로잡을 길이 없었다.
새 호실은 마지막 뒤에 붙인다(0 으로 두면 맨 앞에 끼어든다).

그 밖(층 필터 · 등록 · 수정 · 삭제 · 호실유형 공통코드 이름 표시 · 상태 딱지)은
모두 있었다. Blazor 쪽이 나은 자리도 있다 — 건물을 바꾸면 층 고르개를 비운다
(원본은 다른 건물의 층이 붙은 채로 남았다).

**함께 고친 곳**: `FuneralModels.cs`(`Room.SortOrder`)

### 5. `/funeral/building/device` — 기기 관리 · **보완**

원본: `funeral/building/device/` 15개 파일 (화면 + 서랍 3탭 + 컴포저블)

원본의 서랍 세 탭(**화면 표시** · **하드웨어** · **장비 정보**)은 Blazor 에도 모두
있다 — 속성 마흔 칸, 하드웨어 설정(음량·밝기·자동 전원·재시작 시각), 장비 정보.
원격 명령(켜기·끄기·재시작)에 **새 판 확인**까지 Blazor 가 더 갖고 있다.

**빠져 있던 것 — 실시간 상태.** 장비가 붙고 떨어질 때 `DeviceHub` 가
`DeviceStatusChanged` 를 방송하는데 **아무도 듣지 않았다.** 목록을 읽은 시점의
상태가 그대로 굳어, 실제로는 켜져 있는 장비가 꺼짐으로 남았다. CLAUDE.md 의
남은 일에도 적혀 있던 항목이다.

**어떻게 붙였나 — 연결은 하나다(`DeviceStatusRelay`, 싱글턴).**
원본은 **브라우저마다** 허브에 붙었다(관리자 열 명이면 연결 열 개). 포털은
서버라서 한 번만 붙고 열려 있는 화면들에 나눠 줄 수 있다. 허브가 인증 없이
받으므로(플레이어가 붙는 곳이다) 싱글턴이어도 신원이 섞이지 않는다.

- 첫 구독자가 생길 때 붙고, 마지막이 나가도 끊지 않는다 — 화면을 들락거릴
  때마다 붙었다 끊으면 그 사이 방송을 놓친다
- 못 붙어도 예외를 올리지 않는다. 상태는 조회한 시점 값으로 남고 자동
  재연결이 계속 시도한다
- **방송을 받아도 목록을 다시 읽지 않는다.** 원본은 0.5초로 묶어 재조회했는데,
  정전 복구처럼 한 건물이 동시에 켜지면 그 한 번이 수십 장비 조회가 된다.
  바뀐 것은 상태 한 칸이라 들고 있는 자료에서 그 칸만 고친다

**함께 고친 곳**: `Directory.Packages.props`·`csproj`(SignalR 클라이언트,
**장례식장 모듈만** 참조) · `FuneralModule`(싱글턴 등록)

**확인.** 빌드·테스트 통과. 실제 방송 수신은 못 봤다 — 빈소 장비가 붙어야 한다.

### 6~8. `/funeral/building/{video,audio,background}` — 영상 · 음원 · 배경 · **보완**

원본: `funeral/building/video/index.vue`(337줄) · `audio`(307줄) · `background`(125줄)

세 화면은 한 표(`media_sources`)를 `SourceType` 으로 갈라 보는 것이라 Blazor 는
`MediaSourceList` 한 벌로 모아 두었다. 그래서 이번 보완도 세 화면에 함께 걸린다.

**빠져 있던 것 둘**

1. **변환 상태.** 서버는 `Status` · `ErrorMessage` · `ConversionCommand` ·
   시작·완료 시각을 주는데 **모델에 칸 자체가 없었다.** 재처리 단추만 있고
   「왜 안 나오는지」를 볼 길이 없었다 — 올린 직후 변환 중인 것과 변환에
   실패한 것이 화면에서 똑같아 보였다. 딱지로 보여 주고, 실패한 것은 마우스를
   올리면 서버가 준 오류와 **실행한 ffmpeg 명령**이 나온다(따질 때 그 줄이 필요하다).
2. **재생·보기.** 원본에는 재생 단추와 창이 있었다. **변환본을 먼저 튼다**
   (`HasWebm`) — 원본이 브라우저가 못 여는 형식이라 서버가 변환본을 만들어
   두는 것이므로, 원본을 먼저 주면 「소리만 나고 화면이 검다」가 된다.
   음원은 `<audio>`, 영상은 `<video>`, 배경·장식은 `<img>` 다.

**함께 고친 곳**: `FuneralModels.cs`(`MediaSource` 에 변환 관련 일곱 칸) ·
`MediaSourceList.razor` · `funeral.css`(`.fn-preview__player`)

**남은 차이(의도적).** 원본 영상 화면은 목록 칸에 썸네일을 크게 깔고 클릭하면
확대했다. Blazor 는 작은 썸네일 칸 + 재생 창이다 — 그리드 줄 높이가 고정이라
칸 안에서 사진이 납작해진다(건물 화면과 같은 사정).

### 9. `/funeral/building/decoration` — 장식 관리 · **그대로**

원본: `funeral/building/decoration/index.vue` (120줄)

목록·미리보기·수정·삭제·올리기가 모두 있다(`MediaSourceList` 공용). 6~8번에서
더한 재생 창과 변환 상태도 함께 걸린다. 머리말의 원본 경로 표기만 바로잡았다
(`funeral/device/decoration` → `funeral/building/decoration`).

### 10. `/funeral/building/music-build` — 건물별 음원 · **보완**

원본: `funeral/building/music-build/index.vue` (185줄)

**빠져 있던 것 셋** — 전부 「손이 얼마나 가느냐」의 문제다.

1. **전체 선택 · 전체 해제.** 건물이 스물이 넘는 곳이 있다. 하나씩 누르는 것과
   전부 켜고 몇 개만 끄는 것은 손이 크게 다르다
2. **배정 개수(`n / 총`)**. 몇 곳에 걸렸는지 세어 보지 않으면 알 수 없었다
3. **첫 음원 자동 선택.** 들어오면 빈 화면이라 무엇을 해야 하는지 알 수 없었다

**의도적 차이.** 원본은 왼쪽 음원 표 + 오른쪽 건물 체크 목록의 두 판이다.
Blazor 는 위에 음원 고르개 한 칸 + 아래 건물 표다 — 두 판으로 나누면 좁은
화면에서 양쪽이 다 잘린다. 고르는 값과 동작은 같다.

### 11. `/funeral/room-status` — 빈소현황 · **보완**

원본: `funeral/building/status/index.vue` (533줄 + 모듈 여럿)

이 화면은 **재설계를 거친 자리**다(D-RS1~8). 카드에서 직접 고치던 것을 각 화면으로
보내고, 대신 서버가 한 번에 주는 값(장비 상태·출상 취소 진입점·기간 조건)을
살렸다. 그 차이들은 화면 머리말에 까닭과 함께 적혀 있어 그대로 둔다.

**빠져 있던 것 — 스스로 새로 고치지 않았다.** 이 화면은 **상황판으로 띄워 둔다**.
사람이 새로 고침을 누르러 오지 않는다는 뜻이다. 원본은 60초 폴링 + 실시간 방송
두 가지를 함께 썼는데 Blazor 는 둘 다 없어서, 띄워 둔 화면이 몇 시간 전 상태로
남았다.

- 60초 폴링(방송을 놓쳤을 때의 보험) + `DeviceStatusChanged` ·
  `RoomAssignmentChanged` 구독. 방송이 몰릴 때는 0.8초로 묶어 한 번만 읽는다
  (정전 복구처럼 한 건물이 동시에 켜지면 방송이 수십 개 온다)
- **조용히 읽는다.** `LoadAsync` 로 읽으면 아무도 안 만졌는데 안내 줄이 깜빡이고
  「조회 결과가 없습니다」가 떴다 사라진다 — 상황판에서 그것은 누가 눌렀다는
  신호로 읽힌다. 조용한 조회가 실패해도 마지막으로 성공한 화면을 그대로 둔다
- 화면을 닫으면 시계와 구독을 놓는다. 빠뜨리면 닫은 뒤에도 60초마다 조회가 나간다

**함께 고친 곳**: `DeviceStatusRelay` 에 `SubscribeAssignments` 추가
(호실 배정 방송. 고인 등록·이동·출상·출상 취소가 모두 이 방송을 낸다)

### 12. `/funeral/deceased` — 고인 관리 · **보완**

원본: `funeral/building/deceased/` 12개 파일 (목록 + 서랍 + 폼 조각 일곱)

**빠져 있던 것 — 사진.** 원본의 폼 조각 일곱 중 `deceased-photo-form` 이 통째로
없었다. 서버는 `MemorialPhotoFileId` · `MemorialPhotoUrl` · `FamilyPhotoGroupId` 를
주고받고 있었고 Blazor 모델에도 칸이 있었는데, **화면에 올리는 자리가 없어서
포털에서는 영정사진을 넣을 수 없었다.** 빈소 화면이 그 사진을 띄우는 것이
이 시스템의 중심 기능이다.

- **영정사진**(한 장) — `file/upload` 로 바로 올린다. 3:4 세로 틀로 미리 보여
  준다(빈소 화면이 그 비율이라, 가로 사진을 올렸다는 것을 저장 전에 알아야 한다)
- **추모 앨범**(여러 장) — 2번에서 만든 `ImageGroup`. 입구 모니터·분향소 화면이
  순서대로 돌린다. 원본과 같은 상한 20장, 같은 업무 구분(`DECEASED`)
- 「빼기」는 **연결만 끊는다.** 파일은 FileServer 에 남긴다 — 플레이어가 옛
  주소를 캐시에 들고 있을 수 있다

**아직 남은 것 — 시설·호실 이력 편집.** 원본에는 `deceased-facilities-form`
(안치실·염습실 배정)과 `deceased-rooms-form`(호실 배정 이력)이 있다. 서버 DTO 와
Blazor 모델에는 `Facilities` · `Rooms` 가 있으나 편집 자리가 없다. 호실 **이동**은
목록에서 되므로 업무가 막히지는 않는다. 다음 차례에 붙인다.

### 13~16. 정보 · 통계 화면 넷 · **보완**

| 메뉴 | 원본 | 결과 |
|---|---|---|
| `/funeral/info/room-history` 호실 이력 | 380줄 | 사진 · 성별 · 나이 칸을 더했다 |
| `/funeral/info/deceased-search` 고인 조회 | 180줄 | 사진 · 종교 · **상태** 칸을 더했다 |
| `/funeral/stat/billing` 정산 | 201줄 | **퇴실** · 항목 수 칸을 더했다 |
| `/funeral/stat/room-usage` 호실 사용 | 157줄 | **층** 칸을 더했다 |

빠져 있던 것이 전부 **표의 칸**이다. 서버는 그 값을 다 주고 있었고 모델에도
칸이 있었다 — 표에 안 걸려 있었을 뿐이라 오류가 나지 않는다.

몇은 그냥 정보가 아니다.

- **사진**(호실 이력 · 고인 조회) — 이름이 같은 분이 있을 때 사람이 가릴 수
  있는 것은 얼굴이다. 조회 화면에서 특히 그렇다
- **상태**(고인 조회) — 이 화면은 지난 고인까지 함께 나오는 자리다. 「지금
  빈소에 계신 분」과 끝난 건을 가릴 칸이 없었다
- **퇴실**(정산) — 비어 있으면 아직 쓰는 중이다. 사용일수만 보면 그것을 알 수
  없어 「왜 이 건은 금액이 안 늘지」가 된다
- **층**(호실 사용) — 호실 이름이 건물마다 겹친다(101호가 여럿이다)

요약 타일·기간 조건·상세 펼치기는 네 화면 모두 이미 있었다.

### 17~18. `/funeral/help/qna` · `/funeral/help/faq` — Q&A · F.A.Q · **보완**

원본: `funeral/help/qna/index.vue`(509줄) · `faq/index.vue`(478줄)

**빠져 있던 것 — 서식.** 두 화면 다 원본은 **서식 편집기**(RichEditor)로 쓰고
`v-html` 로 그렸다. Blazor 는 여러 줄 입력으로 쓰고 **태그를 걷어낸 글자**만
보여 주고 있었다. 정제 없이 HTML 을 그리는 것이 위험하다는 판단은 맞지만,
그 사이 두 가지가 망가져 있었다.

- 저장된 글의 **서식이 통째로 죽었다** — 번호 매긴 절차, 굵은 글씨가 실제로 들어 있다
- 새로 쓴 글은 **줄바꿈이 사라진다** — HTML 로 그려지므로 개행이 무시된다.
  글이 한 덩어리로 붙어 나온다
- 고치러 열면 **태그가 글자로 보인다**

공지 본문이 이미 쓰던 길을 그대로 쓴다 — 쓰기는 `DxHtmlEditor`,
그리기는 `NoticeHtml.Sanitize`(서식 편집기가 뱉는 태그만 남기고 script·이벤트
속성·낯선 스킴을 걷어낸다). 쓰지 않게 된 `PlainText` 도우미는 지웠다.

### 19. `/funeral/setting/environment` — 환경설정 · **의도적 차이**

원본: `funeral/setting/environment/index.vue` (655줄 + catalog + 부품 넷)

원본은 **vben 프레임워크의 개인 환경설정 판**이다(테마·레이아웃·탭바·애니메이션
등 70여 항목). Blazor 셸에는 그 설정 대부분이 존재하지 않고, 개인 설정은
**헤더의 테마 서랍**(DevExpress 테마 22조합 · 크기 셋)이 맡는다. 이 화면은 지금
무엇을 어디서 고르는지 안내한다. 화면 머리말에 그 판단과 결정 대기(D8:
테마를 계정에 저장할지)가 적혀 있어 그대로 둔다.

**남는 차이**: 원본에는 **언어**와 **시간대** 고르개가 있었다. Blazor 셸에는
아직 그 둘이 없다(메뉴 제목의 다국어는 서버가 옮긴다). 셸의 기능이라 이 화면의
몫이 아니고, 남은 일로 적어 둔다.

### 20. `/funeral/help/archive` — 자료실 · **보완**

원본: `funeral/help/archive/index.vue` (582줄)

Q&A·F.A.Q 와 같은 문제였다. 설명이 **서식 편집기로 쓰던 HTML** 인데 여러 줄
입력으로 바뀌어 있었고, 게다가 **목록 어디에도 설명이 안 나왔다** — 자료실
설명에는 「어떤 장비에 쓰는 판인지」 같은 안내가 들어 있다. 상세 펼침에
정제해서 그리고, 쓰기는 `DxHtmlEditor` 로 되돌렸다.

### 21~24. 현황판 넷 — 간편 · 모바일 · 장례정보 · 장례현황 · **보완**

원본: `status/simple`(177줄) · `status/mobile`(90줄) · `status/funeral-info`(85줄) ·
`status/funeral-status`(150줄)

**빠져 있던 것 — 자동 갱신.** 넷 다 안내 데스크·로비·사무실에 **띄워 두는**
화면이고, 원본은 30초 시계를 돌렸다. 옮기면서 그것이 빠져 **아침에 띄운 상태로
굳어 있었다.** 빈소현황(11번)과 같은 성격의 결함이다.

**공용 뼈대로 만들었다 — `AutoRefreshPage`(Blazor Common).** 화면 넷이 저마다
시계를 돌리면 「조용히 읽기」 규칙이 한 곳에서 빠진다.

- `LoadAsync` 로 다시 읽지 않는다. 안내 줄이 깜빡이고 「조회 결과가 없습니다」가
  저절로 떴다 사라지는데, **상황판에서 그 깜빡임은 누가 눌렀다는 신호로 읽힌다**
- 자동 조회가 실패하면 마지막으로 성공한 화면을 그대로 둔다. 사람이 보고 있지
  않을 때 빨간 줄을 띄워 봐야 소용이 없다
- 화면을 닫으면 시계를 놓는다. 빠뜨리면 닫은 뒤에도 조회가 계속 나간다

### 25~28. 고인현황 · 내 정보 · 미리보기 · 업무설정 · **보완 셋 · 그대로 하나**

| 메뉴 | 결과 |
|---|---|
| `/funeral/status/deceased-status` 고인 현황 | **종교** 칸을 더했다. 장례 절차와 제단 차림이 그 값에 따라 다르다 |
| `/funeral/info/my-info` 내 정보 | 「프로필 수정」·「업무 설정」으로 가는 단추를 되살렸다. 안내 문구가 **환경설정을 가리키고 있었는데** 이 목록을 고치는 곳은 업무 설정이다 |
| `/funeral/info/preview` 미리보기 | 카드에 장비명·코드·유형·최근 접속과 「크게 보기」를 더했다. 그림만 있으면 **어느 장비인지 알 수 없다** — 호실이 없는 장비(입구 안내·키오스크)가 특히 그렇다 |
| `/funeral/setting/work-options` 업무 설정 | **그대로.** 스위치를 누르는 즉시 저장하는 것까지 같다 |

**장례식장 모듈 28개 화면 점검을 마쳤다.**

---

## 포털관리 (Admin)

### 29~34. 공통코드 · 메타데이터 · 다국어 · 계정 · 메뉴 · 공지 · **그대로**

| 메뉴 | 원본 | 판정 |
|---|---|---|
| `/admin/system/common-code` | 22줄(껍데기) | **그대로.** Blazor 598줄이 더 갖췄다 |
| `/admin/system/metadata` biz-select 설정 | 81줄 | 그대로 |
| `/admin/system/i18n` 다국어 | 137줄 | 그대로 |
| `/admin/system/account` 계정 | 376줄 | **그대로.** 생일·음력·축하 대상은 Blazor 가 더 갖고 있다 |
| `/admin/system/menu` 메뉴 | 696줄 | 아래 참고 |
| `/admin/notice` 공지 | 679줄 | 그대로(제목·내용·기간·팝업·전체공개·순서·첨부 모두) |

**메뉴 관리의 남은 차이** — 원본은 표에서 **끌어서 옮기고 순서를 바꿨다**
(`menu/move`). Blazor 는 상위 메뉴와 순서를 편집 폼에서 값으로 고른다. 화면
머리말에 그 판단이 적혀 있고, 할 수 있는 일은 같다(끌기가 편할 뿐이다).
업무가 막히지 않아 그대로 둔다.

### 35. `/admin/release` — 배포 도구 · **보완**

원본: `portal/release/index.vue` (574줄)

**빠져 있던 것 — 진행 중 자동 갱신.** 원본은 배포가 도는 동안 2초마다 되물어
진행 기록을 흘려 보여 주었다. Blazor 는 「폴링은 보고 있지 않은 탭에서도 돈다」는
걱정으로 아예 빼 두었는데, 그 결과 **배포가 끝났는지 보려면 조회를 계속 눌러야
했다**(배포는 몇 분씩 걸린다).

**도는 것이 있을 때만** 시계를 켠다(3초). 대기·진행 중인 실행이 없어지면 시계를
끈다 — 놀고 있는 화면은 게이트웨이를 두드리지 않는다. 걱정과 쓸모를 둘 다 지킨다.
열어 둔 진행 기록 창도 함께 맞춘다.

**배포를 거는 단추는 여전히 없다**(결정 D2). 운영 서버에서 스크립트를 돌리는
일이라 사람이 위험을 정해 주어야 한다.

### 36~40. 권한 · 회사 · 부서 · 회사사용자 · **그대로**

| 메뉴 | 원본 | 판정 |
|---|---|---|
| `/admin/auth/role` 역할 | 241줄 | 그대로 |
| `/admin/auth/user-role` 사람롤 | 653줄 | 그대로 |
| `/admin/auth/menu-role` 메뉴롤 | 844줄 | 그대로 |
| `/admin/company/list` 회사 | 176줄 | 그대로 |
| `/admin/company/dept` 부서 | 319줄 | 3번 참고(셀 즉시 편집만 차이) |
| `/admin/company/user` 회사 사용자 | 780줄 | 아래 참고 |

**회사 사용자의 남은 차이** — 원본은 오른쪽에 **부서 트리**를 두고 사람을 끌어다
부서에 넣었다. 그 일은 지금 **조직도**(`/admin/company/org-chart`)가 한다 —
이번 점검의 1번 항목에서 끌어 놓기와 이동 팝업을 모두 갖췄다. 같은 일을 하는
자리를 둘로 만들지 않는다.

### 41. `/admin/push/dashboard` — 푸시 현황 · **보완**

원본: `portal/system/push/dashboard.vue` (356줄)

**빠져 있던 것 — 열람 쪽 지표 전부.** 발송 성공률·추이·실패 사유는 있었는데
원본이 함께 보여 주던 **도달·열람 요약**, **많이 읽힌 알림**, **사람별 수신·열람**이
없었다. 서버에는 셋 다 있었다(`push-engagement-stats` ·
`top-performing-messages` · `user-engagement-stats`).

**보낸 것과 읽힌 것은 다른 이야기다.** 성공률만 보면 「보냈으니 됐다」로 끝나고,
아무도 안 읽는 알림을 계속 보내게 된다. 사람별 표는 **알림이 닿지 않는 사람**을
찾는 자리다(열람률이 0 이면 구독이 끊겼을 수 있다).

**남은 차이**: 원본의 추이 **차트**(echarts)와 CSV 내려받기. 추이는 표로 있고,
표는 `CommGrd` 가 엑셀 내보내기를 이미 준다.

### 42. `/admin/push/setting` — 알림 설정 · **남은 일 있음**

원본: `portal/system/push/setting.vue` (580줄)

스위치 셋(푸시·이메일·날씨) · 시험 발송 · 기기 목록 · 기기 빼기는 모두 있다.
**구독 등록·해제**(브라우저 푸시 권한 요청)가 없다 — 서비스 워커와 PWA 매니페스트가
아직 안 옮겨졌기 때문이다(CLAUDE.md 의 남은 일, 결정 D10). 이 화면만의 문제가
아니라 셸의 기능이라 여기서 손대지 않는다.

### 43~44. `/admin/status/server` · `/admin/status/deploy` — **보완**

원본: `portal/system/server-status/index.vue`(1,242줄) · `deploy-status/index.vue`(412줄)

**빠져 있던 것 — 자동 갱신.** 둘 다 **장애와 빌드를 지켜보는 자리**인데 사람이
조회를 눌러야 값이 바뀌었다. 원본은 각각 10초·30초마다 되물었다. 같은 간격으로
되돌렸다.

**정밀 확인(`ai/health/deep`)은 자동으로 부르지 않는다.** 유료 제공자에게 실제
질문을 보내는 호출이라 10초마다 돌면 돈이 나간다 — 사람이 누를 때만 나간다.
옛 화면도 자동 갱신에서는 목록만 다시 읽었다.

그 밖(서비스 응답·의존성·AI 제공자·컨테이너·GitHub 실행·러너)은 모두 있었다.

### 45~50. 프로필 · 푸시 로그 · 알림 이력 · 가입 신청 · jin114 · 묶음 화면 · **그대로**

| 메뉴 | 판정 |
|---|---|
| `/admin/profile` 프로필 | **그대로.** 원본의 일곱 조각(기본·계정·비밀번호·보안·알림·고정탭·사진)이 모두 있다. 고정탭의 끌어놓기를 검색+↑↓ 로 바꾼 판단은 화면 머리말에 있다(미고정 138건이라 끌기가 오히려 어렵다) |
| `/admin/push/logs` 푸시 로그 | 그대로(대상·제목·내용·결과·실패 사유·기간) |
| `/admin/push/history` 알림 이력 | 그대로 |
| `/admin/system/signup` 가입 신청 | **원본 없음.** Blazor 에서 새로 만든 화면이다 |
| `/admin/status/jin114` | iframe 한 장. 원본과 같다 |
| 묶음 화면(권한·공통·회사·푸시·상태) | 길잡이 화면. 원본에 대응이 없다 |

**포털관리 모듈 점검을 마쳤다.**

---

## 소개사이트·AI (Site) · 생활과환경 (LifeEnv)

### 51~52. `/site/ai/chat` · `/site/inquiries` — **그대로**

- **AI 대화**(원본 410줄) — 알맹이는 헤더 서랍과 같은 공용 부품(`AiChatPanel`)이다.
  복사해 두면 한쪽에만 고침이 들어간다
- **사이트 문의**(원본 291줄) — 목록·상세·상태 전환이 모두 있다. **답장 보내기**만
  없다(메일이 나가는 되돌릴 수 없는 동작, 결정 D2). 화면 머리말에 그 판단이 있다

### 53. `/life/weather/forecast` — 시간대별 예보 · **보완**

원본: `life/weather/forecast.vue` (426줄)

**빠져 있던 것 둘**

1. **그래프.** 원본은 기온·강수확률·습도·풍속 따위를 그림으로 보여 주었다.
   예보는 「몇 시에 몇 도」보다 **어디서 꺾이는가**를 보는 자료다 — 표만 있으면
   스물네 줄을 눈으로 훑어야 한다. 기온(선)과 강수확률(막대)을 한 그림에 둔다.
   **축이 둘이다** — 기온은 영하까지 가고 확률은 0~100 이라 한 축에 두면 한쪽이
   납작해진다. 원본은 항목마다 그림을 따로 그렸는데, 실제로 함께 보는 것은 그 둘이다
2. **주간 예보(오늘~10일).** `GetMidTermForecastAsync` 가 **클라이언트에 이미
   있었는데 화면이 부르지 않았다.** 시간대별 표는 오늘·내일에 그친다.
   중기 자료가 늦게 들어오는 때가 있어, 주간이 실패해도 시간대별은 보여 준다

### 54~63. 생활과환경 나머지 열 · **그대로**

기상 현황판 · 특보 · 대응 이력 · 수집 이력 · 행사 · 관측 지역 · 판단 기준 ·
생일 목록 · 생일 달력 · 생일 메시지 — 원본 기능이 모두 있다.

---

## 프로젝트관리 (ProjMng)

### 64~95. 프로젝트관리 32화면 · **그대로 (한 건 차이)**

이 모듈은 보통 CRUD 가 아니다. **프로시저 이름과 조건을 실어 보내면 결과와
컬럼 메타가 함께 오는** 구조라, 화면이 맞는지 보는 기준은 「같은 프로시저를 같은
조건으로 부르는가」다. 표본으로 확인한 것들:

| 화면 | 프로시저 | 결과 |
|---|---|---|
| 소스 정보 | `sp_dev_srcinfo_exec` · `sp_dev_srcinfo_dtl_exec` | 같다 |
| 할 일 | `sp_home_todo_exec` · `sp_home_todo_make` | 같다 |
| DB 도구 | `sp_dev_db_prop_exec` (`db_pkey='sp_fmt'`) | 같다 |
| 일정표 | `sp_proj_wbs_exec` (`schedule_type='SCHEDULE'`) | 같다 |

원본 화면들이 47~252줄로 작은 것도 같은 까닭이다 — 업무 로직이 DB 에 있다.
Blazor 쪽이 대체로 더 길다(권한·실패 처리·엑셀을 부품이 아니라 화면에서 걸어서).

**남은 차이 — 일정표의 달력 보기.** 원본은 월 달력에 일정을 얹고 **끌어다 놓아
날짜를 바꿨다.** Blazor 는 기간 프리셋 탭 + 표이고, 날짜는 칸에서 고친다.
고칠 수 있는 값은 같지만 **달력으로 보는 맛은 없다.** 간트 둘을 `DxScheduler`
타임라인으로 옮긴 전례(D4)가 있어 같은 방식으로 붙일 수 있다 — 다음 차례로 남긴다.

---

## 헬프데스크 (HelpDesk)

### 이 모듈의 사정 — 46화면 중 39개가 「서버가 준 대로」다

옮길 때 대부분을 `AutoGrid`(서버가 준 칸을 그대로 그리는 표)로 두었다. 그 판단
자체는 기록되어 있다 — 엔드포인트가 마흔 개라 DTO 를 마흔 개 만들면 백엔드가
칸을 더할 때마다 화면이 아니라 DTO 를 고치러 가게 된다.

**다만 표로 대신할 수 없는 화면들이 섞여 있었다.** 원본 줄 수와 견주면 바로 보인다.

| 원본 | 줄 | Blazor | 줄 | 무엇이 사라졌나 |
|---|---|---|---|---|
| `schedule/index` | 845 | ScheduleAll | 45 | **달력 · 등록 · 수정 · 완료 토글** |
| `util/mc-model` | 873 | McModelList | 43 | 모델 편집기 |
| `report/monitoring` | 771 | ReportMonitoring | — | 차트 · 기간 비교 |
| `request/manage` | 562 | RequestManage | — | 배정 · 상태 전환 |
| `monitor/sm` | 479 | SmMonitor | 44 | 실시간 갱신 · 상세 |
| `util/binary-parser` | 496 | BinaryParser | — | 전문 파싱 입력 |

### 96. `/helpdesk/schedule/all` — 전체 일정 · **다시 씀**

원본: `helpdesk/schedule/index.vue` (845줄)

**표 한 장만 남아 있었다.** 원본은 달력이다 — 칸을 눌러 등록하고, 일정을 끌어다
날짜를 바꾸고, 완료를 그 자리에서 토글한다. Blazor 에서는 **일정을 등록할 수도
고칠 수도 없었다.** 일정은 「무엇이 있나」보다 **「언제 비어 있나」**를 보는
자료인데, 표로는 그 빈자리가 보이지 않는다.

- 달력은 `DxScheduler` 다 — 프로젝트 간트를 타임라인으로 옮길 때 쓴 것과 같은
  부품이라 **새 라이브러리를 얹지 않았다**(D4 전례)
- **종일 일정으로 그린다.** 서버가 날짜만 다루므로 시간표로 그리면 모든 일정이
  자정에 붙어 한 줄로 겹친다
- 표 탭도 남겼다. 「이번 달」이 아니라 전부를 훑을 때는 표가 빠르다
- 기본이 **미완료만** — 지난 일정이 쌓이면 달력이 회색으로 덮인다(원본과 같다)

### 97. `/helpdesk/schedule/my` — 내 일정 · **다시 씀**

원본도 전체 일정과 **한 파일**이었다(`myOnly` 하나로 갈랐다). 여기서도 같은
화면을 쓰고 조회에 회사 조건만 붙인다. `schedules/my` 는 서버에 없다 — 일정을
회사로만 거를 수 있어 「내 것」은 소속 회사 일정 + 공통 일정이 된다.

### 98~99. `/helpdesk/hanju/{health-check,collection-status}` — **보완**

원본은 둘 다 **60초마다 되물었다**(`setInterval`). 설비가 살아 있는지, 수집이
멈추지 않았는지 보는 화면이라 띄워 두는 자리인데, 사람이 새로 고침을 눌러야
값이 바뀌고 있었다. 같은 간격으로 되돌렸다.

표 자체는 `AutoGrid` 로 둔다 — 프로시저가 돌려주는 칸이 조회 종류마다 다르고
늘어나기도 해서, 칸을 박아 두면 새 칸이 조용히 사라진다.

### 100. `/helpdesk/util/binary-parser` — 바이너리 파서 · **보완**

원본: `helpdesk/util/binary-parser.vue` (496줄)

**빠져 있던 것 — 보관한 전문.** 설비 모델마다 전문 샘플을 저장해 두고 꺼내
쓴다(`mc-models/{id}/samples`). 한 번 겪은 전문을 다시 손으로 찾아 붙여 넣지
않으려고 만든 장치다. **서버에도 자료 타입(`BinarySample`)에도 다 있었는데
화면에 꺼내는 자리가 없었다.**

- 모델을 고르면 그 모델의 보관 전문이 나온다
- 불러오기는 한 건을 따로 읽는다 — **목록 응답에는 내용이 빠져 있다**
- 보관은 이름이 그대로면 덮어쓰고 바꾸면 새로 만든다(원본과 같은 가름)
- 보관함을 못 읽어도 파서는 쓸 수 있다
