name: test
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/build-push-action@v6
        env:
          DEVEXPRESS_LICENSE: ${{ secrets.DEVEXPRESS_LICENSE }}
        with:
          secret-envs: |
            dxlicense=DEVEXPRESS_LICENSE
