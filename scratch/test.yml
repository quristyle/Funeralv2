name: test
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/build-push-action@v6
        env:
          MY_SECRET: ${{ secrets.MY_SECRET }}
        with:
          secrets: |
            "dxlicense=env=MY_SECRET"
